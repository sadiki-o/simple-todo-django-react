# Django Project Name

## Description

This is a Django application built with django rest framework since we are using React in the frontend and not Django templates plus DRF simplifies json web token generation, validation and user management in general. It includes the following features:

- User authentication using JWT with refresh token mechanism included(signup, login, logout).
- CRUD operations on todos list


You can install Django and other dependencies by following the instructions below.

## Installation
Create env:

```bash
python -m venv venv
```
activate env:

```bash
source venv/bin/activate
```
install packages:

```bash
pip install -r requirements.txt
```
run migrations:

```bash
python todoproject/manage.py migrate
```
run server:
```bash
python todoproject/manage.py runserver
```

### Clone the repository:

```bash
git clone https://github.com/yourusername/yourprojectname.git
cd yourprojectname
